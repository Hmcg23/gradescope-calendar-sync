{
  "name": "gradescope-calendar-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that syncs Gradescope deadlines into a dedicated Google Calendar",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "id": "node scripts/extension-id.mjs",
    "icons": "node scripts/make-icons.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm test"
  },
  "devDependencies": {
    "linkedom": "^0.18.5"
  }
}
